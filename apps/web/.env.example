# Copiar para .env.local e ajustar se necessário.
# A API em desenvolvimento usa por defeito https://localhost:7225 (ver apps/web/app/lib/apiConfig.ts).

NEXT_PUBLIC_API_URL=https://localhost:7225

# Opcional: permitir botão «Limpar dados» no painel Admin fora de production (só com API em Development).
# NEXT_PUBLIC_ALLOW_CLEAR_DATA=true
